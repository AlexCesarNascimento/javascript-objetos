const produto = { nome: 'Produto', preco: 1.8 };

// valores
console.log(Object.values(produto));

// chaves
console.log(Object.keys(produto));

// entradas: valores e chaves
console.log(Object.entries(produto));